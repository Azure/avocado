version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    ignore:
      # dependencies

      # Locked to "^15.6.1", for compat with typescript version
      - dependency-name: "@types/node"

      # Locked to "^9.3.5", for compat with cjs
      - dependency-name: "glob"

      # Locked to "^1.4.2", for compat with typescript version
      - dependency-name: "node-object-hash"

      # Locked to "^15.4.1", since newer majors include breaking changes that may impact us
      - dependency-name: "yargs"

      # devDependencies

      # Updated manually
      - dependency-name: "prettier"
      - dependency-name: "tslint"

      # Locked to "~3.6.5", for compat with our TS code
      - dependency-name: "typescript"

      # Updated manually, for compat with typescript version
      - dependency-name: "@types/jest"

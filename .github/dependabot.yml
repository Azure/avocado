version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    ignore:
      # dependencies

      # Pinned to the most popular versions
      - dependency-name: "@azure/swagger-validation-common"
      - dependency-name: "@ts-common/async-iterator"
      - dependency-name: "@ts-common/fs"
      - dependency-name: "@ts-common/json"
      - dependency-name: "@ts-common/json-parser"
      - dependency-name: "@ts-common/string-map"

      # Locked to "^9.3.5", the latest version compatible with cjs
      - dependency-name: "glob"

      # devDependencies

      # Updated manually
      - dependency-name: "prettier"
      - dependency-name: "tslint"
      - dependency-name: "typescript"

      # Updated manually, for compat with typescript version
      - dependency-name: "@types/jest"
      - dependency-name: "@types/yargs"
